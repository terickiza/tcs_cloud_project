apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: devops-api-ing
  namespace: default
  annotations:
    kubernetes.io/ingress.class: "nginx"
spec:
  rules:
  - host: app.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: devops-api-svc
            port:
              number: 80
  - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: devops-api-svc
                port:
                  number: 80